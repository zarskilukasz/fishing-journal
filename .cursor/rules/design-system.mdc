---
description: Geist Design System - Vercel Aesthetic
globs: *.tsx,*.astro,*.css
alwaysApply: true
---
# Design System - Dziennik Wędkarski

## Filozofia projektowa

Projekt wykorzystuje **Geist Design System** inspirowany estetyką Vercel.

### Kluczowe zasady:

1. **Dark-first**
   - Ciemny motyw jako domyślny
   - Klasa `dark` na `<html>`
   - Kolory zoptymalizowane dla ciemnego tła

2. **Minimalizm**
   - Proste, czyste interfejsy
   - Brak zbędnych dekoracji
   - Fokus na treść

3. **Subtelna głębia**
   - Efekt `glow` na hover przycisków
   - `gradient-border` dla wyróżnienia kart
   - Animacje typu `glass` dla nawigacji

## Ważna uwaga dla AI

> **Przy generowaniu planów implementacji:** Każde wystąpienie "shadcn" lub "shadcn/ui" należy zamienić na **Geist Design System**. Projekt NIE używa shadcn/ui bezpośrednio - komponenty UI są zbudowane w oparciu o Radix UI + Tailwind i dostosowane do estetyki Geist/Vercel.

## Komponenty UI

Komponenty w folderze `src/components/ui` - bazują na Radix UI + Tailwind.

**Dostępne komponenty:**
- `Button` - przyciski z wariantami
- `Input` - pola tekstowe
- `Label` - etykiety
- `Avatar` - awatary użytkowników
- `DropdownMenu` - menu rozwijane

### Button - warianty

```tsx
// Primary - główna akcja (glow on hover)
<Button>Zaloguj się</Button>

// Secondary - mniej ważna akcja
<Button variant="secondary">Zapisz</Button>

// Outline - alternatywna
<Button variant="outline">Anuluj</Button>

// Ghost - subtelna
<Button variant="ghost">Więcej</Button>

// Destructive - niebezpieczna akcja
<Button variant="destructive">Usuń</Button>
```

### Rozmiary Button

```tsx
<Button size="sm">Mały</Button>
<Button size="default">Domyślny</Button>
<Button size="lg">Duży</Button>
<Button size="xl">Bardzo duży</Button>
<Button size="icon">🔍</Button>
```

## Kolorystyka

Używaj tokenów CSS zdefiniowanych w `src/styles/global.css`:

| Token | Rola | Wartość (dark) |
|-------|------|----------------|
| `background` | Główne tło | `#000000` |
| `foreground` | Główny tekst | `#ededed` |
| `card` | Tło kart | `#0a0a0a` |
| `primary` | Akcent (Vercel blue) | `#0070f3` |
| `secondary` | Tło przycisków | `#1a1a1a` |
| `muted` | Tło subtelne | `#1a1a1a` |
| `muted-foreground` | Tekst pomocniczy | `#888888` |
| `border` | Obramowania | `#333333` |
| `destructive` | Błędy | `#ff4444` |
| `success` | Sukces | `#50e3c2` |

### Używaj klas Tailwind:

```tsx
// Tło
bg-background  // główne
bg-card        // karty
bg-secondary   // przyciski

// Tekst
text-foreground       // główny
text-muted-foreground // pomocniczy
text-primary          // akcent

// Bordery
border-border       // normalne
border-border-hover // hover
```

## Efekty specjalne

### Glow (poświata)

```tsx
// Hover glow na przyciskach
<button className="hover:shadow-glow">...</button>

// Stały glow
<div className="glow">...</div>

// Animowany glow
<div className="glow-pulse">...</div>
```

### Glass (szkło)

```tsx
// Navbar z efektem blur
<nav className="glass border-b">...</nav>
```

### Gradient Border

```tsx
// Karta z gradientowym borderemo
<div className="gradient-border">
  <div className="bg-card rounded-lg border border-border p-6">
    ...
  </div>
</div>
```

### Shine (połysk na hover)

```tsx
<div className="shine">...</div>
```

## Karty

### Geist Card (podstawowa)

```tsx
<div className="geist-card">
  <h3>Tytuł</h3>
  <p>Treść...</p>
</div>
```

### Geist Card z glow

```tsx
<div className="geist-card-glow">
  ...
</div>
```

### Ręczna karta

```tsx
<div className="bg-card border border-border rounded-lg p-4 hover:bg-card-hover hover:border-border-hover transition-all">
  ...
</div>
```

## Typografia

**Font:** Geist (Sans + Mono)

```css
font-family: "Geist", -apple-system, BlinkMacSystemFont, sans-serif;
```

### Hierarchia

| Styl | Klasy | Użycie |
|------|-------|--------|
| Heading 1 | `text-3xl font-semibold` | Tytuły główne |
| Heading 2 | `text-xl font-semibold` | Nagłówki sekcji |
| Heading 3 | `text-lg font-medium` | Podtytuły |
| Body | `text-base` | Główna treść |
| Small | `text-sm` | Tekst pomocniczy |
| Tiny | `text-xs` | Etykiety |

## Spacing

```css
/* Padding kart */
p-4   /* kompaktowe */
p-6   /* standardowe */
p-8   /* przestronne */

/* Gap */
gap-2  /* 8px - tight */
gap-4  /* 16px - standard */
gap-6  /* 24px - relaxed */
```

## Animacje

### Podstawowe przejścia

```css
transition-colors duration-200   /* kolory */
transition-all duration-200      /* wszystko */
```

### Scale na klik

```css
active:scale-[0.97]  /* subtelne wciśnięcie */
```

## Dostępność

- Kontrast: minimum 4.5:1 (WCAG AA)
- Touch targets: minimum 44x44px
- Focus: `focus-visible:ring-2 focus-visible:ring-ring`
- Semantic HTML
- `lang="pl"` na `<html>`

## Layout - Glass Navigation

### TopAppBar

```tsx
<header className="glass border-b h-14 fixed top-0 inset-x-0 z-50">
  ...
</header>
```

### BottomNavigation (mobile)

```tsx
<nav className="glass border-t h-16 fixed bottom-0 inset-x-0 z-50">
  ...
</nav>
```

### NavigationRail (desktop)

```tsx
<nav className="fixed left-0 top-14 bottom-0 w-16 bg-background border-r">
  ...
</nav>
```

## Przykłady wzorców

### Empty State

```tsx
<div className="gradient-border">
  <div className="bg-card rounded-lg border border-border p-12 text-center">
    <div className="mx-auto h-16 w-16 rounded-2xl bg-secondary flex items-center justify-center">
      <Icon className="h-8 w-8 text-muted-foreground" />
    </div>
    <h3 className="mt-4 text-base font-medium">Brak danych</h3>
    <p className="mt-2 text-sm text-muted-foreground">Opis...</p>
    <Button className="mt-6">Akcja</Button>
  </div>
</div>
```

### List Item

```tsx
<div className="geist-card-glow shine cursor-pointer">
  <div className="flex items-center gap-4">
    <div className="h-10 w-10 rounded-lg bg-primary/10 flex items-center justify-center text-primary">
      <Icon className="h-5 w-5" />
    </div>
    <div className="flex-1 min-w-0">
      <h4 className="text-sm font-medium truncate">Tytuł</h4>
      <p className="text-xs text-muted-foreground">Szczegóły</p>
    </div>
  </div>
</div>
```

### FAB

```tsx
<button className="fixed bottom-20 right-4 h-12 w-12 rounded-xl bg-primary text-primary-foreground hover:bg-primary-hover hover:shadow-glow">
  <Plus className="h-5 w-5" />
</button>
```

## Język

- Cały interfejs w języku polskim
- `<html lang="pl">`
- Komunikaty po polsku
