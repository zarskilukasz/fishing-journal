---
interface Props {
  text?: string;
  class?: string;
}

const { text = "Twój osobisty asystent wędkarski", class: className } = Astro.props;
---

<p class:list={["max-w-md text-center text-base text-muted-foreground leading-relaxed", className]}>
  {text}
</p>
